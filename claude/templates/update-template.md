# Update: <name>

## What exists today
<Link to the `features/<name>.md` this update applies to, or describe the
current behavior if there's no matching feature file.>

## What should change
<The specific, concrete change requested — be precise about what's staying
the same vs. what's different.>

## Why
<Optional: the reasoning, if it helps the agent make the right judgment
calls on anything not explicitly spelled out.>

## Files likely affected
<Paths, if known — otherwise leave blank and let the agent find them. Note
whether it's the web app root or `mobile-app/`.>

## Out of scope
<Anything that might look related but should NOT change.>
